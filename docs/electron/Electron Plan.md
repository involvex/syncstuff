electrom app full enhanced : fix tray symbol + custom menu bar / nav on top , settings menu with quick access in tray ,
modern material theme , push notifiations

Also update the workflows add workflow for desktop build .

The upload build artefakts ( add for electron build a porteable build , ) apk + .exe porteable should be attached to tags

-> High performance sync with sync stuff server / cloud sync / local sync
-> Application optimized for performance and battery life
-> Material design 3
-> Push notifications
-> Make sure no background processes are running when not needed / No looped background processes / Sync Process Call should be on demand + Asynchronous / Function should return a promise / Await the function call

Customizable Sync Process / Hotkeys for quick sync files or clipboard + Keys for quick oben App /
